# GoBeauty Admin (Scaffold)

Roman Urdu recap:
- Ye folder React + Vite + TS + Tailwind ke liye hai.
- Protected admin panel: dashboard, categories/services/users/providers/bookings/coupons/finance/notifications/reviews/settings.
- API base: `http://localhost:4000/api` (server folder). Auth via JWT.

Next steps (manual):
1) `npm create vite@latest admin -- --template react-ts` chalayein ya yarn/pnpm se. (Ya main yahan files bana doon agar aap kahe.)
2) Tailwind + TanStack Query install karein; env me API URL set karein.
3) Pages/components ko current HTML UI (gradients/glass) se map karein.
